name: "Firmware Update Failure"
description: "Send UpdateFirmware command and simulate failure during download"
station_count: 1
timeout_seconds: 60
tags: [device-management, firmware, failure]

steps:
  - name: "Send BootNotification"
    type: send
    action: BootNotification
    message_type: REQUEST
    payload:
      stationId: "{{stationId}}"
      firmwareVersion: "1.2.0"
      stationModel: "SimStation"
      stationVendor: "OneStopPay"
      serialNumber: "{{serialNumber}}"
      bayCount: 4
      bootReason: "power_on"

  - name: "Wait for boot accepted"
    type: wait_for
    action: BootNotification
    message_type: RESPONSE
    timeout_ms: 5000

  - name: "Set firmware behavior to fail at downloading"
    type: set_behavior
    action: update_firmware
    config:
      accept_rate: 1.0
      failure_rate: 1.0
      failure_at_stage: "downloading"

  - name: "Wait for UpdateFirmware command"
    type: wait_for
    action: UpdateFirmware
    message_type: REQUEST
    timeout_ms: 30000

  - name: "Wait for FirmwareStatusNotification (Failed)"
    type: wait_for
    action: FirmwareStatusNotification
    message_type: EVENT
    timeout_ms: 30000

  - name: "Assert firmware failure reported"
    type: assert
    assertion: payload_field
    params:
      field: "status"
      value: "Failed"
