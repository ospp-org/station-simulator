name: "Session Timeout"
description: "Start a session and let it timeout based on max duration, verify auto-stop"
station_count: 1
timeout_seconds: 120
tags: [session, timeout]

steps:
  - name: "Send BootNotification"
    type: send
    action: BootNotification
    message_type: REQUEST
    payload:
      stationId: "{{stationId}}"
      firmwareVersion: "1.2.0"
      stationModel: "SimStation"
      stationVendor: "OneStopPay"
      serialNumber: "{{serialNumber}}"
      bayCount: 4
      bootReason: "power_on"

  - name: "Wait for boot accepted"
    type: wait_for
    action: BootNotification
    message_type: RESPONSE
    timeout_ms: 5000

  - name: "Wait for StatusNotification"
    type: wait_for
    action: StatusNotification
    message_type: EVENT
    timeout_ms: 3000

  - name: "Wait for StartService command"
    type: wait_for
    action: StartService
    message_type: REQUEST
    timeout_ms: 30000

  - name: "Assert session active"
    type: assert
    assertion: session_active
    params:
      bay_number: 1
      expected: true

  - name: "Wait for max session duration + buffer"
    type: delay
    duration_ms: 65000

  - name: "Assert session ended (timeout)"
    type: assert
    assertion: session_active
    params:
      bay_number: 1
      expected: false

  - name: "Assert bay available after timeout"
    type: assert
    assertion: bay_status
    params:
      bay_number: 1
      expected: "Available"
